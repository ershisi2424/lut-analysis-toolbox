# LUT分析工具箱 v1.3.0

正式版，适用于 Windows x64。下载 `LUT-Analysis-Toolbox-1.3.0-portable.exe` 后可直接运行，无需安装 Node.js。

## 本版更新

- 修正标准 ImageMagick HaldCLUT 二维像素排列，提升与外部调色软件的互操作性。
- 修正亮度保持算法的零亮度失效和色度幅度误改问题。
- 修正扩展输入域 LUT 的质量步长计算、转换断层风险和偶数样本中位数计算。
- IRE 与检查器曲线支持显示负值区间，同时保留标准 0–100 IRE 参考范围。
- 分析器边框跟随 LUT 数据与混合比例变化，恢复上下两排互补色角点关系。
- 恢复并校正 15% 灰工作背景；滑块值、百分比和默认状态保持同步。
- 预览器保留自定义图片原始像素尺寸，移除 1920×1080 自动降采样和 DPR 二次放大。
- 重采样与转换面板随页面滚动保持在视口工作区，窄屏自动恢复纵向布局。
- 修复 Hald 级别下拉框及其他控件越界；转换摘要同步显示 CUBE 与 Hald PNG 估算。
- 保留 LUT 历史、颜色总结、批量导出、三种插值、高级调整及所有既有功能。

## 精度与格式说明

- `.cube` 解析和导出保留 `DOMAIN_MIN`、`DOMAIN_MAX` 与超出 0–1 的浮点值。
- Hald PNG 是 8 位 RGB 格式，超出 0–1 的值会裁切，且不能保存 `.cube` 输入域元数据；扩展范围请使用 CUBE。
- 自定义预览图保持原始宽高和逐像素映射。Canvas 预览工作在 8 位 sRGB；源文件已有压缩损失或更高位深不能由预览器恢复。

## 构建验证

- JavaScript 语法检查
- LUT 数学与异常输入测试
- 1000 组随机非线性 LUT 独立插值参考比对
- 标准 Hald 像素位置与往返测试
- Electron 登录、导航、三工具页面、历史记录和控件测试
- 原图与 LUT 结果像素一致性、原尺寸图片及宽窄窗口布局测试
- Windows 便携 EXE 唯一性、最小体积和 PE 文件头检查
- SHA-256 校验文件
